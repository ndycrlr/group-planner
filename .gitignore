node_modules/
*.db
*.db-journal
*.db-wal
*.db-shm

# Vercel
.vercel/
# `vercel link` writes a VERCEL_OIDC_TOKEN here, so this must never be tracked.
.env*

# Playwright
.test-pending
.test-tmp/
test-results/
playwright-report/
blob-report/
playwright/.cache/
